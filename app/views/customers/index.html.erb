<%= render 'layouts/c_header' %>
<div class="container">
    <div class="row mt-5"><h3>メンバー一覧</h3></div>
    <div class="row">
    	<% @customers.each do |customer| %>
        <div class="col-lg-3 col-md-6 col-sm-6 mt-5">
        	<div class="card card-starts">
        		<div class="card-header card-header-warning card-header-icon">
        			<div class="card-icon">
        			<%= attachment_image_tag customer, :profile_image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg" %>
        			</div>
        			<h5 class="card-category"><%= link_to customer.last_name,customer_path(customer) %></h5>
					<h5 class="card-title"><%= customer.set_total %><small>rate</small></h5>
				</div>
			</div>
		</div>
		<% end %>
	</div>