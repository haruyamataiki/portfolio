<%= render 'layouts/c_header' %>
 <%= form_for(@team) do |f| %>
    <%= f.attachment_field :team_image %>
                <%= f.text_field :name %>
    <%= f.submit "編集内容を保存する",class:"btn btn-md btn-primary" %>
    <% end %>